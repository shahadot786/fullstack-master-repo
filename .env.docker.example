# Environment Variables for Docker Compose
# Copy this file to .env.docker and update with your values
# Usage: docker-compose --env-file .env.docker up

# ============================================
# General Configuration
# ============================================
NODE_ENV=development
COMPOSE_PROJECT_NAME=nexus

# ============================================
# MongoDB Configuration
# ============================================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password123
MONGO_DATABASE=nexus
MONGO_PORT=27017

# ============================================
# Redis Configuration
# ============================================
REDIS_PORT=6379

# Note: You can also use Upstash Redis instead of local Redis
# In that case, comment out the redis service in docker-compose.yml
# and set REDIS_DATABASE_URI to your Upstash URL:
# REDIS_DATABASE_URI=rediss://default:your-password@your-host.upstash.io:6379

# ============================================
# Backend Configuration
# ============================================
BACKEND_PORT=8000

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production-min-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Email Configuration (for OTP delivery)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@nexus.app

# OTP Configuration
OTP_EXPIRY_MINUTES=10

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# Web Application Configuration
# ============================================
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# ============================================
# Mobile Configuration (if using Docker for Expo)
# ============================================
# EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
# REACT_NATIVE_PACKAGER_HOSTNAME=0.0.0.0
