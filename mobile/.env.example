# API Configuration
# Default API URL (used if platform-specific URLs are not set)
API_BASE_URL=http://localhost:8000/api

# Android-specific API URL (use your computer's local IP address for real devices)
# Find your IP: Mac: ipconfig getifaddr en0 | Windows: ipconfig
API_BASE_URL_ANDROID=http://192.168.1.100:8000/api

# iOS-specific API URL (localhost works for iOS simulator)
API_BASE_URL_IOS=http://localhost:8000/api

# App Configuration
APP_NAME=Nexus
