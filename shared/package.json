{
  "name": "@fullstack-master/shared",
  "version": "1.0.0",
  "description": "Shared types, validation schemas, and utilities for fullstack master repo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["shared", "types", "validation"],
  "license": "MIT",
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
